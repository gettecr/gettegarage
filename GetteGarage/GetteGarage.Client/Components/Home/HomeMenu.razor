
<div class="menu-row css-fade-in")">
    <GameMenuButton Title="WORKSHOP" Subtitle="Projects & Work" Link="/workshop" Delay="0.4s" />
    <GameMenuButton Title="DEV_LOG" Subtitle="Read the Blog" Link="/blog" Delay="0.5s" />
    <GameMenuButton Title="ARCADE" Subtitle="Play Minigames" Link="/arcade" Delay="0.6s" />
    <GameMenuButton Title="CREDITS" Subtitle="About Me" Link="/about" Delay="0.7s" />
    <GameMenuButton Title="COMMUNITY" Subtitle="Discuss" Link="/community" Delay="0.8s" />
</div>

<style>
    .menu-row {
        display: flex;
        flex-direction: row; /* Desktop: Side-by-side */
        gap: 2rem;
        justify-content: center;
        width: 100%;
        max-width: 1200px;
        
    }
    .css-fade-in {
        opacity: 0;
        animation: menuFadeIn 0.5s ease-out forwards;
        animation-delay: 0.4s; /* Wait for the logo animation to finish */
    }

    @@keyframes menuFadeIn {
        0% { opacity: 0; transform: translateY(20px); }
        100% { opacity: 1; transform: translateY(0); }
    }

    /* MOBILE FIX: Switch to Column */
    @@media (max-width: 960px) {
        .menu-row {
            flex-direction: column; /* Stack vertically */
            gap: 1rem;              /* Tighter gaps for mobile */
            padding-bottom: 80px;   /* Extra room at bottom for the Command Menu button */
        }
    }

    .menu-row.visible {
        opacity: 1;
        transform: translateY(0);
    }

</style>
